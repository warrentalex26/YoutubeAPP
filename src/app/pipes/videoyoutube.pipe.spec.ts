import { VideoyoutubePipe } from './videoyoutube.pipe';

describe('VideoyoutubePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoyoutubePipe();
    expect(pipe).toBeTruthy();
  });
});
